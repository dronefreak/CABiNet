# Dataset Configuration
name: cityscapes
config_file: configs/cityscapes_info.json # Relative path (project-root relative)
num_classes: 19
cropsize: [1024, 1024]
dataset_path: ${oc.env:DATA_ROOT,''} # Env override + fallback
ignore_idx: 255
seed: 0
# Class weights are no longer a static per-dataset table — they're computed
# at training startup from a sample of the training set via
# training_config.cls_pw (see configs/train.yaml).
# Note: shuffle is handled in DataLoader, not here
